package oop

// 1. Criou um arquivo(file) Kotlin separado chamado de Database

// 2. criou um OBJECT nomeado de Database (object Database { })
object Database {
    // 3. Primeira propriedade criada chama de NAME (var name = "Desconhecido")
    var name = "Desconhecido"
    // 4. Criou uma variável para ter um usuário logado (var userLogged: User? = null)
        // Aplicado os fundamentos de Nullables
    var userLogged: User? = null

    // Isso faz com que nosso banco de dados tenha um nome e um usuário logado
}