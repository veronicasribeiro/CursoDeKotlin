// main - A primeira função que será executada (o botão de play irá aparecer)
    // entry point (ponto de entrada)
fun main() {
    // Variável imutável
    val product = "Imac"

    println(product)
}

